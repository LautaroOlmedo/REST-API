package customer

import (
	"testing"
)

func Test_NewCustomer(t *testing.T) {
	type testCase struct {
		test          string
		name          string
		email         string
		password      string
		expectedError error
	}
}
